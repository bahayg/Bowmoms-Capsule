<h2>New Post Form:</h2>

<%= form_for @post do |f| %>

Disease <%= f.collection_select(:disease_id, Disease.all, :id, :name) %></br>
Topic: <%= f.collection_select(:topic_id, Topic.all, :id, :title) %></br>
Title: <%= f.text_field :title %></br>
Body: <%= f.text_area :body %></br>

<%= f.submit %></br>
<% end %></br>
